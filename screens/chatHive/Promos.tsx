import { View } from "react-native";
import { MonoText } from "../../components/StyledText";

const Promos = () => {
    return (
        <View>
            <MonoText style={{textAlign: "center", marginTop: 300}} >No Promo Available</MonoText>
        </View>
    );
};

export default Promos;